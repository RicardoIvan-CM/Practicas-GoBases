package main

/*
import "fmt"

type Point struct {
	x, y int
}

func (p *Point) move(xd, yd int) {
	p.x += xd //Es igual a  (*p).x += xd por la desreferenciacion automatica en la operacion selector (x.f)
	p.y += yd
}

func main() {
	p := Point{0, 2}
	fmt.Println(p)
	p.move(1, 2)
	fmt.Println(p)
}
*/
